"""IBM Storage Scale CLI Command Tools."""

from fastmcp import FastMCP

# Create the CLI MCP server (empty for now)
mcp = FastMCP("scale-cli", instructions="IBM Storage Scale CLI command operations")
